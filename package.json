{
  "name": "aicoachellavalley",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "pretest": "node scripts/generate-stats.mjs",
    "test": "node --test scripts/*.test.mjs",
    "prebuild": "node scripts/check-pricing.mjs && npm test",
    "build": "node scripts/build-static-json.cjs && astro build",
    "preview": "astro preview",
    "postbuild": "node scripts/check-ownership.mjs && node scripts/check-amendments.mjs && node scripts/check-node-content.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.3",
    "astro": "^6.1.3"
  }
}
